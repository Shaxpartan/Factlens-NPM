# FactLens NPM v6.7.0 Execution Checkpoint

Implementation authorized on 2026-08-17.

- Release target is exactly 6.7.0.
- Keep existing v6.5.0 verify behavior source-compatible for ordinary callers.
- Add precise runtime diagnostics and richer CLI/SDK capabilities without weakening idempotency or billable-request safety.
- Keep progress forward-only and color-aware.
- Execute on a dedicated work branch derived from this planning head.
- Do not publish automatically and do not move `main` until the coordinated API/NPM release is verified.

## Recovery checkpoint — 2026-08-17

Active NPM branch: `work/v6.7.0-runtime-integration`.
Coordinated API branch: `work/unified-runtime-v84`.
Plan: `docs/superpowers/plans/2026-08-17-v6.7.0-runtime-integration.md`.
Current execution is in final release gates (about 94%).

### Confirmed implemented

- Package metadata is `6.7.0`; Node 18+ support remains.
- `verify()` remains the simple compatibility path while `verifyDetailed()` provides additive response metadata.
- `src/runtime/response-meta.ts` parses `Server-Timing` phases `auth`, `customization`, `core`, `postprocess`, `edge`, `X-FactLens-Edge-Time-Ms`, request ID, client total, outside-network estimate, retry count, and bounded `Retry-After`.
- `FactLensError` can expose safe parsed runtime metadata/retry delay without embedding credentials.
- `usage.getDetailed()` exposes non-billable runtime timing while `usage.get()` preserves its original data shape.
- Transport composes caller AbortSignal with timeout cleanup, retries transient read-only GETs once by default, and does not automatically retry billable Verify POSTs or mutations.
- Progress metadata includes phase/poll timing fields without inventing percentages where the API cannot know completion percent.
- CLI supports total/core timing, `--verbose` runtime phase breakdown, `--trace` safe HTTP/runtime diagnostics, non-billable `factlens doctor`, and result-focused request-detail rendering without Input/Pipeline sections.
- `tests/v6.7.0-release-contract.test.mjs` locks the coordinated safety/timing/CLI/doctor/request-detail contract.
- CI uses `scripts/run-v6.7-runtime-release.mjs`, then tests Node 18, 20 and 22 with `npm ci`, typecheck, full tests, examples and `npm pack --dry-run`.
- The release runner now removes duplicate generated v6.7 README runtime-metadata sections and makes the canonicalizer guard replay-safe.

### Coordinated backend state

- FactLens production Supabase migrations for runtime snapshot, parallel non-AI service leases and request-detail fast path are already applied and were verified service-role-only.
- The live `factlens-api` Edge Function is still the pre-v84 staged-loader wrapper and must be replaced once with the unified v84 bundle.
- The live `factlens-api-console` Edge Function is still the pre-v84 direct request-audit lookup and must be replaced once with the v84 request-detail fast path.
- The API work branch request-detail source was restored to its exact clean canonical blob after detecting a non-idempotent generator artifact; a regression finalizer/test now prevents recurrence.

### Remaining release gates

1. Restore the NPM README to the known clean single-section v6.7 blob on top of the current branch head; do not force over CI commits.
2. Verify the latest API/NPM branch source and available CI evidence; fix only concrete failures.
3. Deploy only the two stale Supabase Edge Functions (`factlens-api`, `factlens-api-console`) exactly once from the final v84 source, preserving current JWT settings.
4. Run non-billable/auth canaries, inspect function logs, and run Supabase security/performance advisors.
5. Write final verification evidence on both work branches.
6. Only after all gates are green, move each repository's `main` once. Do not publish the npm package without an explicit separate instruction to publish.

If execution is interrupted, resume from this checkpoint and the two work branches above. Do not restart the v6.7.0 implementation from scratch.