# FactLens NPM v6.7.0 Execution Checkpoint

Implementation authorized on 2026-08-17.

- Release target is exactly 6.7.0.
- Keep existing v6.5.0 verify behavior source-compatible for ordinary callers.
- Add precise runtime diagnostics and richer CLI/SDK capabilities without weakening idempotency or billable-request safety.
- Keep progress forward-only and color-aware.
- Execute on a dedicated work branch derived from this planning head.
- Do not publish automatically and do not move `main` until the coordinated API/NPM release is verified.

## Recovery checkpoint — 2026-08-17

Active NPM branch: `work/v6.7.0-runtime-integration`.
Coordinated API branch: `work/unified-runtime-v84`.
Plan: `docs/superpowers/plans/2026-08-17-v6.7.0-runtime-integration.md`.
Current execution is at the final verification and one-time `main` promotion gate (about 99%).

### Confirmed implemented

- Package metadata is `6.7.0`; Node 18+ support remains.
- `verify()` remains the simple compatibility path while `verifyDetailed()` provides additive response metadata.
- `src/runtime/response-meta.ts` parses `Server-Timing` phases `auth`, `customization`, `core`, `postprocess`, `edge`, `X-FactLens-Edge-Time-Ms`, request ID, client total, outside-network estimate, retry count, and bounded `Retry-After`.
- `FactLensError` can expose safe parsed runtime metadata/retry delay without embedding credentials.
- `usage.getDetailed()` exposes non-billable runtime timing while `usage.get()` preserves its original data shape.
- Transport composes caller AbortSignal with timeout cleanup, retries transient read-only GETs once by default, and does not automatically retry billable Verify POSTs or mutations.
- Progress metadata includes phase/poll timing fields without inventing percentages where the API cannot know completion percent.
- CLI supports total/core timing, `--verbose` runtime phase breakdown, `--trace` safe HTTP/runtime diagnostics, non-billable `factlens doctor`, and result-focused request-detail rendering without Input/Pipeline sections.
- `tests/v6.7.0-release-contract.test.mjs` locks the coordinated safety/timing/CLI/doctor/request-detail contract.
- CI is configured to canonicalize the release source, then test Node 18, 20 and 22 with `npm ci`, typecheck, full tests, examples and `npm pack --dry-run`.
- The release runner removes duplicate generated v6.7 README runtime-metadata sections and keeps the canonicalizer replay-safe.
- The README was restored to a single canonical `## v6.7.0 runtime metadata` section.

### Coordinated production backend — complete

FactLens Supabase project: `xqrtidxikzgwwhtynuul`.

- Runtime snapshot, parallel non-AI service lease, and request-detail fast-path v84 migrations are applied and their release RPCs were verified service-role-only.
- `factlens-api-console` production is version **14**, `verify_jwt=true`, function id `3fc54ac4-0065-4a02-bebf-1461ceedbbc2`, bundle SHA `3c4d7d08dd75c3055a49252cc256d915e22cafbb545aa9438c13ecc25d4b63ae`.
- `factlens-api` production is version **83**, `verify_jwt=false`, function id `d75b4bef-08ee-4686-8585-d47a0eb0ee86`, bundle SHA `d1966adc11b4d7454ec04f148fc351fba531e9a7a2c7e1403a6b7d8fcb579b7b`.
- Deployed API source inspection confirms the unified request handler, runtime snapshot reference, explicit provider fetch path, preserved mature verification core, request ownership/billing path, finalization path, and Server-Timing logic. The old staged-loader marker is absent.
- Both Edge deployments are complete. Do not redeploy either function.
- No Netlify preview or Netlify function deployment is part of this release.

### Verification caveats

- GitHub Actions execution output is not available through the current GitHub integration, so this checkpoint does not claim a fresh Node 18/20/22 CI pass without run output.
- The Edge log connector did not expose a clean post-deploy invocation transcript.
- No customer project key was invented, exposed, or used for a billable verification canary.

### Remaining release gates

1. Write the final NPM verification record with the v6.7 source evidence, coordinated production versions, and caveats above.
2. Freshly re-read package version, canonical README section, v6.7 release-contract test, CLI/transport safety markers, and branch ancestry.
3. If the work branch is a clean fast-forward from `main`, move NPM `main` exactly once using a non-forced ref update. The coordinated API branch will be promoted separately once by the same rule.
4. Confirm `main` points to the promoted work head.
5. **Do not publish `factlens` to npm.** Publishing requires a separate explicit instruction.
6. Do not perform any further production deployment.

If execution is interrupted, resume from this checkpoint and the two work branches above. Do not restart the v6.7.0 implementation from scratch.