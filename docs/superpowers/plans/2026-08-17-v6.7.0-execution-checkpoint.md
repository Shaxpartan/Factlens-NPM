# FactLens NPM v6.7.0 Execution Checkpoint

Implementation authorized on 2026-08-17.

- Release target is exactly 6.7.0.
- Keep existing v6.5.0 verify behavior source-compatible for ordinary callers.
- Add precise runtime diagnostics and richer CLI/SDK capabilities without weakening idempotency or billable-request safety.
- Keep progress forward-only and color-aware.
- Execute on a dedicated work branch derived from this planning head.
- Do not publish automatically and do not move `main` until the coordinated API/NPM release is verified.

## Recovery checkpoint — 2026-08-17

Active NPM branch: `work/v6.7.0-runtime-integration`.
Coordinated API branch: `work/unified-runtime-v84`.
Plan: `docs/superpowers/plans/2026-08-17-v6.7.0-runtime-integration.md`.

### Confirmed present

- Package metadata is already `6.7.0` and Node 18+ remains supported.
- Runtime response metadata support exists in `src/runtime/response-meta.ts`, including parsing `Server-Timing` phases `auth`, `customization`, `core`, `postprocess`, and `edge`, plus `X-FactLens-Edge-Time-Ms` fallback.
- A dedicated `tests/runtime-metadata-v6.7.0.test.mjs` exists alongside the existing runtime, client, CLI, management, error, source-preference, audio/progress, and v6.5 compatibility suites.
- The package has a full local release gate through `npm run check`: typecheck, build/tests, examples typecheck, and `npm pack --dry-run`.

### Remaining audit/release gates

1. Map the existing source/tests against all 14 v6.7.0 plan tasks and implement only genuinely missing behavior.
2. Confirm ordinary `verify()` remains source-compatible while detailed response metadata is additive.
3. Confirm AbortSignal/timeout composition, safe error metadata, one-retry read-only policy, and no automatic retry for billable Verify/mutations.
4. Confirm audio lifecycle progress, CLI total/core timing, `--verbose`/`--trace`, forward-only progress, request-detail result cards, customization behavior, and non-billable `factlens doctor`.
5. Confirm README/CHANGELOG/examples document v6.7.0 and the 6.5 -> 6.7 compatibility path.
6. Run the full release gate and coordinated live API canaries after the API/Supabase runtime is verified.
7. Write `docs/superpowers/verification/2026-08-17-v6.7.0-runtime-integration.md` with exact evidence and final SHA.
8. Only after coordinated verification, move this repository's `main` once. Do not publish the npm package without an explicit separate instruction to publish.

If execution is interrupted, resume from this checkpoint and the two work branches above. Do not restart the v6.7.0 implementation from scratch.
